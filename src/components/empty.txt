import { motion } from 'framer-motion';
import WavingGhost from '../components/ui/WavingGhost';

const About = () => {
    return (
        <section id="about" className="flex items-center justify-center relative py-32 z-10">
            <div className="container mx-auto px-4">
                <div className="flex flex-col md:flex-row items-center justify-between gap-16 max-w-6xl mx-auto">

                    {/* Left Column: Animation */}
                    <motion.div
                        initial={{ opacity: 0, x: -50 }}
                        whileInView={{ opacity: 1, x: 0 }}
                        transition={{ duration: 0.8 }}
                        viewport={{ once: true }}
                        className="w-full md:w-1/2 flex justify-center"
                    >
                        <WavingGhost />
                    </motion.div>

                    {/* Right Column: Content Card */}
                    <motion.div
                        initial={{ opacity: 0, x: 50 }}
                        whileInView={{ opacity: 1, x: 0 }}
                        transition={{ duration: 0.8 }}
                        viewport={{ once: true }}
                        className="w-full md:w-1/2 p-8 md:p-12 bg-black/40 backdrop-blur-md border border-white/10 rounded-2xl shadow-xl"
                    >
                        <h2 className="text-4xl md:text-5xl font-bold mb-8">
                            About <span className="text-blue-500">Me</span>
                        </h2>

                        <div className="space-y-6 text-gray-300 leading-relaxed text-lg">
                            <p>
                                I am a passionate software engineer with a keen eye for design.
                                My journey in web development started with a curiosity for how things work on the internet,
                                and it has evolved into a career where I build seamless, user-centric interfaces.
                            </p>
                            <p>
                                I specialized in modern frontend technologies like React, TypeScript, and Tailwind CSS.
                                I believe in writing clean, maintainable code and solving complex problems with simple, elegant solutions.
                            </p>
                            <p>
                                When I'm not coding, you can find me exploring new design trends, contributing to open source,
                                or gaming.
                            </p>
                        </div>
                    </motion.div>
                </div>
            </div>
        </section>
    );
};

export default About;


.
